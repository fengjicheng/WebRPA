export const moduleReferenceContent = `# 模块速查表

本页按侧边栏分类逐个列出模块，用于「我知道要做什么，但不知道该用哪个模块」时反查。
每类先说用途与典型组合，再列模块清单；需要完整配置说明和实战案例时，跳转到对应的专题文档。

> 查模块的三种方式：① 本页按分类找；② 左侧模块搜索框直接搜中文、拼音或首字母（输入 \`dkwy\` 就能搜到「打开网页」）；③ 问 AI 小助手「我要做 XX，用哪个模块」。

---

## 网页自动化补充模块

### 网页导航

配合「打开网页」使用的页面级操作。iframe 是最容易踩坑的一类：页面里嵌套的 iframe 属于独立文档，不先切进去，里面的元素怎么找都找不到。

| 模块 | 用途 |
|------|------|
| 关闭网页 | 关掉当前标签页，一般放在工作流末尾释放资源 |
| 切换iframe | 进入指定 iframe，之后的元素操作都作用在这个 iframe 内 |
| 切换回主页面 | 从 iframe 退回主文档，忘了退出会导致后续找不到主页面元素 |
| 网页是否加载完成 | 把加载状态写入变量，供条件判断用；不阻塞流程 |

### 网页元素交互与查询

「元素存在判断」「元素可见判断」返回布尔值到变量，是写健壮流程的基础：先判断再操作，避免元素没出现就点击而报错。

| 模块 | 用途 |
|------|------|
| 勾选框 | 勾选 / 取消勾选复选框，按目标状态设置而非盲目点击 |
| 元素存在判断 | 元素是否存在于 DOM，结果存入变量 |
| 元素可见判断 | 元素是否可见（存在但被隐藏时为 false） |
| 表格数据提取 | 把 HTML 表格整体读成列表，比逐个单元格取值快得多 |

### 网页数据采集：网络监听

抓不到页面上渲染的数据时（数据是 JS 动态填的、或分页走的是接口），改成监听接口响应更稳。三个模块要按顺序成套使用。

| 模块 | 用途 |
|------|------|
| 开始网络监听 | 打开监听，之后页面发出的请求都会被记录 |
| 等待API请求 | 阻塞等待匹配 URL 的请求完成，把响应体写入变量 |
| 停止网络监听 | 关闭监听并释放资源 |

典型顺序：开始网络监听 → 点击元素（触发请求）→ 等待API请求 → 停止网络监听。

### DP 反检测自动化

常规浏览器自动化被网站风控识别时改用这套（底层是 DrissionPage）。它更接近真人操作，代价是可用的高级能力少一些。这一套模块自成体系，不要和常规网页模块混用同一个页面。

| 模块 | 用途 |
|------|------|
| DP打开页面 | 启动 DP 浏览器并打开网址 |
| DP点击元素 | 点击（支持 CSS / XPath 定位） |
| DP输入文本 | 向输入框写入文本 |
| DP获取文本 | 读取元素文本到变量 |
| DP获取HTML | 读取元素或整页 HTML 到变量 |
| DP执行JS | 在页面上下文执行 JavaScript |
| DP等待元素 | 等待元素出现，避免操作过早 |
| DP滚动页面 | 滚动页面，用于触发懒加载 |
| DP关闭浏览器 | 关闭 DP 浏览器，释放进程 |

---

## 桌面与屏幕自动化补充模块

### 图像识别与文字点击

网页/桌面元素都定位不到时的兜底手段（例如 Flash、Canvas、远程桌面画面）。缺点是依赖屏幕分辨率与主题，换机器容易失效，优先级应排在元素定位之后。

| 模块 | 用途 |
|------|------|
| 点击文本 | 在屏幕上 OCR 找到指定文字并点击它 |
| 悬停文本 | 找到文字并把鼠标悬停其上（用于触发悬浮菜单） |

### 屏幕与录制

| 模块 | 用途 |
|------|------|
| 桌面录屏 | 录制屏幕为视频文件，常用于给失败的流程留证据 |

### 桌面应用控件操作

这一整套是 Windows 桌面软件自动化的核心（底层走 UIAutomation）。用法有固定套路：先用「启动应用」或「连接应用」拿到应用句柄变量，再用「查找控件」拿到控件句柄变量，之后所有操作都基于这两个句柄。

| 模块 | 用途 |
|------|------|
| 窗口状态控制 | 最小化 / 最大化 / 还原窗口 |
| 输入文本到控件 | 向输入框类控件写入文本（比模拟键盘稳） |
| 获取控件文本 | 读取控件显示的文字到变量 |
| 设置控件值 | 直接设置控件的值（适用于下拉框、数值框等） |
| 操作单选按钮 | 选中指定单选按钮 |
| 发送快捷键 | 向控件发送组合键（如 Ctrl+S） |
| 获取控件属性 | 读取控件的任意 UIAutomation 属性到变量 |
| 批量抓取列表 | 一次性把列表 / 表格控件的所有行读成列表 |
| 应用状态快照 | 抓取应用当前的控件树摘要，排查定位失败时很有用 |
| XPath查找控件 | 用类 XPath 语法定位控件，适合层级深、无唯一标识的场景 |
| 选中并提取文字 | 选中控件内文本并提取，用于无法直接读取文本的控件 |
| 滚动控件 | 滚动列表 / 面板控件，用于加载更多内容 |

> 定位不到控件时的排查顺序：① 用「应用状态快照」看控件树里它到底叫什么；② 用「等待控件」而不是立即查找；③ 换「XPath查找控件」按层级定位。

### 手机自动化

| 模块 | 用途 |
|------|------|
| 点击文本 | 在手机屏幕上找到指定文字并点击（需先连接设备） |

---

## 流程控制与触发器补充模块

### 流程控制

| 模块 | 用途 |
|------|------|
| 无限循环 | 一直循环直到被「跳出循环」终止。索引变量默认 \`loop_index\`（注意「循环」模块默认是 \`index\`），务必在循环体内写好退出条件 |
| 断言/检查点 | 校验变量 / 元素 / 表达式是否符合预期，失败时按配置中断或仅记录。做自动化测试时用它产出可读的结论 |
| 强制停止工作流执行 | 立即结束整条工作流，可填停止原因写进日志 |
| 运行其它工作流 | 把另一个工作流文件当子流程整体执行，用于复用与拆分大流程 |

### 触发器

| 模块 | 用途 |
|------|------|
| 手势触发器 | 用摄像头识别手势来触发后续流程，可设置置信度阈值与超时；识别结果写入变量 |

---

## 数据处理补充模块

### 列表操作

列表类模块统一约定：输入是「列表变量名」，输出写进「结果变量」。它们不修改原列表，方便串联。

| 模块 | 用途 |
|------|------|
| 列表导出 | 把列表写成文件（CSV / JSON 等） |
| 列表求和 | 数值列表求和 |
| 列表求平均值 | 数值列表求平均 |
| 列表求最大值 | 取最大值 |
| 列表求最小值 | 取最小值 |
| 列表排序 | 升序 / 降序排序，支持按字段排序 |
| 列表去重 | 去掉重复元素 |
| 列表截取 | 按起止下标取子列表 |
| 列表反转 | 顺序反转 |
| 列表查找 | 查找元素并返回下标或元素本身 |
| 列表计数 | 统计元素出现次数 |
| 列表过滤 | 按条件筛选出符合的元素 |
| 列表映射 | 对每个元素做同一种转换 |
| 列表扁平化 | 把嵌套列表拉平成一层 |
| 列表分组 | 按固定长度切成多段，常用于分批处理 |
| 列表去空 | 去掉空字符串 / null 元素 |
| 列表交集 | 两个列表共有的元素 |
| 列表并集 | 两个列表合并去重 |
| 列表差集 | 只在第一个列表里的元素 |
| 列表笛卡尔积 | 两个列表的所有组合 |
| 列表随机打乱 | 打乱顺序 |
| 列表采样 | 随机取 N 个元素 |
| 列表转字符串（高级） | 按分隔符拼接，可指定前后缀与字段 |

### 字典操作

| 模块 | 用途 |
|------|------|
| 字典键列表 | 取出所有键成列表 |
| 字典合并 | 合并两个字典，后者覆盖前者同名键 |
| 字典过滤 | 按条件保留部分键值对 |
| 字典映射值 | 对每个值做同一种转换 |
| 字典反转 | 键值互换 |
| 字典排序 | 按键或值排序 |
| 字典深拷贝 | 复制一份互不影响的字典 |
| 字典路径取值 | 用 \`a.b.c\` 这类路径取嵌套值，比逐层取值省事 |
| 字典扁平化 | 把嵌套字典拉平成单层，键用路径表示 |

> 处理接口返回的 JSON 时，「字典路径取值」+「字典扁平化」这对组合最常用：前者精准取一个字段，后者一次看清整个结构。

### 数学与统计

| 模块 | 用途 |
|------|------|
| 求余 | 取余数 |
| 开次方 | 开任意次方（默认平方根） |
| 求次方 | 求任意次幂 |
| 对数运算 | 支持自定义底数 |
| 指数运算 | 自然指数 |
| 排列组合 | 计算排列数 / 组合数 |
| 数字范围限制 | 把数值夹到 [最小, 最大] 区间内，防越界 |
| 随机数生成（高级） | 支持整数 / 浮点、指定分布与精度 |
| 数据归一化 | 把一组数缩放到 0~1 |
| 数据标准化 | 按均值和标准差做 Z-score 变换 |

---

## Excel 补充模块

Excel 一整套模块的完整教学在「Excel 自动化」专题里，这里只补充速查表未覆盖的部分。

| 模块 | 用途 |
|------|------|
| 添加工作表 | 新建 sheet |
| 删除工作表 | 删除指定 sheet |
| 插入行 | 在指定位置插入空行 |
| 插入列 | 在指定位置插入空列 |
| 删除列 | 删除指定列 |
| 读取公式/值 | 关键配置是「读取内容」：选「值」拿计算结果，选「公式」拿 \`=SUM(A1:A9)\` 这样的公式文本。读出来是公式串而不是数字，就是这里选错了 |
| 设置超链接 | 给单元格加链接 |
| 设置批注 | 给单元格加批注 |
| 获取第一个空列 | 找到第一个空列的位置，用于追加数据 |
| 获取第一个空单元格 | 找到第一个空单元格，用于定位写入点 |

---

## 数据库模块

七套数据库的模块结构完全一致，学会一套就会全部：**连接 → 操作 → 断开**。连接模块把连接句柄写进一个变量，后续操作模块靠这个变量找到连接，末尾务必断开释放。

> 通用注意事项：
> - 查询类模块把结果写成「列表 + 字典」结构，可直接接「遍历列表」逐行处理。
> - 写入类模块返回受影响行数，可用「断言/检查点」校验是否符合预期。
> - SQL 里拼接变量时注意注入风险，优先用模块提供的参数化字段而不是手工拼字符串。
> - 流程异常退出也要断开连接，建议把断开放在工作流末尾并配合「强制停止工作流执行」前的清理。

### MySQL

| 模块 | 用途 |
|------|------|
| MySQL连接数据库 | 建立连接，写入连接变量 |
| MySQL查询数据 | 执行 SELECT，结果写入变量 |
| MySQL执行SQL | 执行任意 SQL（建表、批量更新等） |
| MySQL插入数据 | 按表名 + 字段插入，不用手写 SQL |
| MySQL更新数据 | 按条件更新 |
| MySQL删除数据 | 按条件删除 |
| MySQL关闭连接 | 释放连接 |

### Oracle

| 模块 | 用途 |
|------|------|
| Oracle连接 | 建立连接 |
| Oracle查询 | 执行查询 |
| Oracle执行SQL | 执行任意 SQL |
| Oracle插入数据 | 插入记录 |
| Oracle更新数据 | 更新记录 |
| Oracle删除数据 | 删除记录 |
| Oracle断开连接 | 释放连接 |

### PostgreSQL

| 模块 | 用途 |
|------|------|
| PostgreSQL连接 | 建立连接 |
| PostgreSQL查询 | 执行查询 |
| PostgreSQL执行SQL | 执行任意 SQL |
| PostgreSQL插入数据 | 插入记录 |
| PostgreSQL更新数据 | 更新记录 |
| PostgreSQL删除数据 | 删除记录 |
| PostgreSQL断开连接 | 释放连接 |

### SQL Server

| 模块 | 用途 |
|------|------|
| SQL Server连接 | 建立连接 |
| SQL Server查询 | 执行查询 |
| SQL Server执行SQL | 执行任意 SQL |
| SQL Server插入数据 | 插入记录 |
| SQL Server更新数据 | 更新记录 |
| SQL Server删除数据 | 删除记录 |
| SQL Server断开连接 | 释放连接 |

### SQLite

无需服务端，连接的是本地文件，适合做工作流自己的中间存储。

| 模块 | 用途 |
|------|------|
| SQLite连接 | 打开数据库文件（不存在会创建） |
| SQLite查询 | 执行查询 |
| SQLite执行SQL | 执行任意 SQL |
| SQLite插入数据 | 插入记录 |
| SQLite更新数据 | 更新记录 |
| SQLite删除数据 | 删除记录 |
| SQLite断开连接 | 关闭文件 |

### MongoDB

文档型数据库，操作对象是集合（collection）和文档（document），不用写 SQL。

| 模块 | 用途 |
|------|------|
| MongoDB连接 | 建立连接 |
| MongoDB查询 | 按条件查文档 |
| MongoDB插入 | 插入文档 |
| MongoDB更新 | 更新文档 |
| MongoDB删除 | 删除文档 |
| MongoDB断开连接 | 释放连接 |

### Redis

键值存储，常用于跨工作流传递状态、做去重集合、做简单计数。

| 模块 | 用途 |
|------|------|
| Redis连接 | 建立连接 |
| Redis读取 | 按 key 取值 |
| Redis写入 | 按 key 写值（可设过期） |
| Redis删除 | 删除 key |
| Redis哈希读取 | 读取哈希表中的字段 |
| Redis哈希写入 | 写入哈希表字段 |
| Redis断开连接 | 释放连接 |

---

## 文档与文件补充模块

### PDF 处理

| 模块 | 用途 |
|------|------|
| PDF旋转页面 | 按角度旋转指定页（扫描件方向不对时用它纠正） |

### 文档格式转换

这一整批走的是同一套转换引擎，配置模式统一：输入路径 + 输出路径。转换质量取决于源文件规范程度，复杂排版的 Word 转 Markdown 会丢样式，属正常现象。

| 模块 | 用途 |
|------|------|
| Markdown转HTML | 生成网页片段 |
| HTML转Markdown | 把网页正文转成 Markdown |
| Markdown转PDF | 生成 PDF（适合出报告） |
| Markdown转Word | 生成 .docx |
| Word转Markdown | 提取 Word 正文为 Markdown |
| HTML转Word | 网页转 .docx |
| Word转HTML | .docx 转网页 |
| Markdown转EPUB | 生成电子书 |
| EPUB转Markdown | 电子书转 Markdown |
| LaTeX转PDF | 编译 LaTeX 源文件 |
| RST转HTML | reStructuredText 转网页 |
| Org转HTML | Org-mode 转网页 |

---

## 媒体处理补充模块

### 图像编辑

| 模块 | 用途 |
|------|------|
| 图片去色 | 转灰度图 |
| 图片圆角化 | 给图片加圆角（可设圆角半径） |

### 视频处理

| 模块 | 用途 |
|------|------|
| 视频旋转 | 按角度旋转视频 |
| 视频倍速 | 加速 / 减速播放 |
| 截取帧 | 抽取某一时刻的画面为图片 |
| 添加字幕 | 把字幕文件烧进视频或作为软字幕 |
| 调整分辨率 | 缩放视频尺寸 |

### 音频处理

| 模块 | 用途 |
|------|------|
| 调节音量 | 放大 / 减小音量 |

### 媒体格式转换

| 模块 | 用途 |
|------|------|
| 视频转音频 | 提取音轨为音频文件 |
| 视频转GIF | 转成 GIF（注意时长越长文件越大） |

---

## AI 能力补充模块

### AI 视觉操作

| 模块 | 用途 |
|------|------|
| AI视觉操作 | 给一句自然语言指令（如「点击登录按钮」），由 AI 看屏幕截图判断该点哪里并执行。元素定位与图像识别都失效时的兜底方案，准确率依赖所用模型 |

### AI 生成

| 模块 | 用途 |
|------|------|
| AI生成图片 | 按提示词生成图片，结果路径写入变量 |
| AI生成视频 | 按提示词生成视频（耗时较长，注意把超时设足） |

### AI 爬虫

| 模块 | 用途 |
|------|------|
| AI智能爬虫 (实验性) | 给网址和一句需求描述，由 AI 决定怎么抓、抓什么 |
| AI元素选择器 (实验性) | 给一句描述让 AI 生成选择器，用于选择器难写的页面 |
| AI单页数据抓取 | 抓单个页面并结构化输出 |
| AI网站链接抓取 | 抓出站点内的链接清单 |
| AI全站数据抓取 | 按规则遍历整站抓取 |

> 标了「实验性」的两个模块结果不稳定，适合探索阶段用；正式流程建议用它们先摸清页面结构，再换成固定选择器的常规模块。

---

## 网络与集成补充模块

### 网络请求

| 模块 | 用途 |
|------|------|
| Webhook请求 | 发送 HTTP 请求（可自定义方法、请求头、请求体），响应体 / 状态码 / 响应头分别写入变量 |

### 消息推送

十几个渠道的配置模式一致：填服务端地址或令牌 + 消息内容。选哪个取决于团队在用什么。

| 模块 | 用途 |
|------|------|
| Discord通知 | 发到 Discord 频道 |
| Telegram通知 | 发到 Telegram（需机器人令牌与会话 ID） |
| Bark通知 | 推到 iOS 的 Bark 客户端 |
| Slack通知 | 发到 Slack 频道 |
| Teams通知 | 发到 Microsoft Teams |
| Pushover通知 | 通过 Pushover 推送 |
| PushBullet通知 | 通过 PushBullet 推送 |
| Gotify通知 | 推到自建 Gotify 服务 |
| Server酱通知 | 通过 Server 酱推到微信 |
| PushPlus通知 | 通过 PushPlus 推到微信 |
| Webhook通知 | 推到任意自定义 Webhook 地址 |
| Ntfy通知 | 推到 ntfy 主题（可自建） |
| Matrix通知 | 发到 Matrix 房间 |
| RocketChat通知 | 发到 Rocket.Chat 频道 |

### 通知与日志

| 模块 | 用途 |
|------|------|
| 系统消息 | 弹出 Windows 系统通知（标题 + 内容），用于提醒本机用户 |

### QQ 机器人

需要先配置好 NapCat 连接，详见「聊天机器人」专题。

| 模块 | 用途 |
|------|------|
| QQ好友列表 | 拉取好友清单到变量 |
| QQ群列表 | 拉取群清单到变量 |
| QQ群成员 | 拉取指定群的成员清单 |
| QQ登录信息 | 读取当前登录账号信息，可用于校验连接是否正常 |

### 飞书与 WPS 多维表格

| 模块 | 用途 |
|------|------|
| 飞书多维表格写入 | 向飞书多维表格追加 / 更新记录 |
| WPS多维表格读取 | 读取 WPS 多维表格数据到变量 |
| WPS多维表格写入 | 向 WPS 多维表格写入数据 |

### SSH 远程

| 模块 | 用途 |
|------|------|
| SSH上传文件 | 本地文件传到远程主机 |
| SSH下载文件 | 远程文件拉到本地 |
| SSH断开连接 | 关闭 SSH 会话 |

### 局域网共享

| 模块 | 用途 |
|------|------|
| 文件夹网络共享 | 把本机文件夹共享到局域网 |
| 文件网络共享 | 共享单个文件 |
| 停止网络共享 | 取消共享 |
| 开始屏幕共享 | 把本机屏幕共享到局域网供他人查看 |

### SAP 自动化

需要先「SAP登录」拿到会话变量，后续模块都靠它定位会话。元素ID用 SAP 自带的脚本录制器获取。

| 模块 | 用途 |
|------|------|
| SAP退出 | 退出登录并结束会话 |
| 关闭警告弹窗 | 关掉挡住流程的警告框 |
| 设置下拉框 | 选择下拉框选项 |
| 切换选项卡 | 切到指定选项卡（元素ID 以 \`tabp\` 开头）；不先切过去，该选项卡内的控件是操作不到的 |
| 导出表格到Excel | 把 GridView 表格导出为 Excel 文件 |

---

## 实用工具补充模块

### 加密与编码

| 模块 | 用途 |
|------|------|
| MD5加密 | 计算 MD5 摘要 |
| SHA加密 | 计算 SHA 系列摘要 |
| URL编解码 | URL 转义 / 反转义 |
| UUID生成器 | 生成 UUID，默认变量名是 \`uuid\` |

### 颜色与时间转换

| 模块 | 用途 |
|------|------|
| RGB转HSV | 颜色空间转换 |
| RGB转CMYK | 转印刷色值 |
| HEX转CMYK | 十六进制色值转印刷色值 |

---

## 找不到想要的模块时

1. 先在左侧搜索框用**功能词**搜（如「压缩」「去重」「截图」），模块名之外的关键词也参与匹配。
2. 再问 AI 小助手：直接描述目标（「我要把 Excel 里的手机号去重后写回」），它会给出模块组合建议。
3. 确认确实没有对应模块时，用「自定义模块」把一段 Python / JS 逻辑封装成模块复用，或用「执行Python脚本」直接写逻辑。
`
